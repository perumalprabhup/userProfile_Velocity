import { UserFiltersPipe } from './user-filters.pipe';

describe('UserFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new UserFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
